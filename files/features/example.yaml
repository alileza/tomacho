scenarios:
  - id: check if alileza.me is up
    steps: 
    - resource: httpclient
      action: send
      arguments: 
        method: GET
        url: /example
        headers:
          Content-Type: application/json
          Accept: application/json

    - resource: httpclient
      action: expect
      arguments: 
        code: 200
        body: '{"hello":"world"}'

    - resource: postgres
      action: expect
      arguments: 
        code: 200
        body: '{"hello":"world"}'